{
  "version": 2,
  "builds": [
    {
      "src": "dist/index.ts",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npm run build",
  "routes": [{ "src": "/(.*)", "dest": "/dist/index.js" }]
}
